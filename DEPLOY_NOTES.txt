Netlify Frontend Deploy Package

Build command: npm run build
Publish directory: dist

Set this env var in Netlify:
VITE_API_BASE_URL=https://<your-render-backend>/api

This package intentionally excludes server/, node_modules/, and .env files.
